// import store from '../store/store'

/**
 * Стандартная функция обработки ошибки при запросе к api
 * @function
 * @param {number} - myParam
 */
// export function throwError (e) {
//   e.status
//     ? store.commit('ADD_ERROR', [e.response.body.error.id, e.response.body.error.message])
//     : store.commit('ADD_ERROR', [404, 'Нет связи с сервером'])
// }
